"""Evaluation set generation and harness for the rag/ hybrid retrieval pipeline.

    python -m rag.eval.generate_eval_set [--sample N]   # (re)build eval_set.jsonl
    python -m rag.eval.run_eval [--strict]              # run the eval harness
"""
